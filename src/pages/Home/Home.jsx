import CanvasContainer from '../../components/CanvasContainer/CanvasContainer';

function Home() {

    return (
        // Home sayfası tanımlanıyor.
        <section id='home' className='home'>
            {/* Canvas Container çağrılıyor. */}
            <CanvasContainer />
        </section>
    )
}

export default Home;